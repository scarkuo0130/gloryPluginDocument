# ID 卡導入計劃書

> 依 `temp/slot_card_portable/index.html`(美術已批准)把整套 ID 卡做進 `index.html`,
> **依 exsample.html 的變體盤點順序(A→I)逐一進行**。
> 工作流:先改 exsample.html demo → local file:// 驗 → 搬 index.html → push → Pages 實測(截圖+實看)。

最後更新:2026-05-28

---

## 1. 三種卡型(真實素材源 = `temp/IDCARD_素材_v1`)

> ⚠️ 校正(2026-05-28):中/小**不是** portable 的烘字原圖(hud_medium/hud_small)。
> 美術 `IDCARD_素材_v1` 對大/中/小**三尺寸都給乾淨框 + 獨立元件**,做法同 C 大卡(框 + HTML 疊字 + 元件 PNG)。portable 那兩張只是展示用烘字圖,不拿來實作。
> 取用素材順序:**先找 `資訊背板_小` → 缺再找 `_中` → 再 `_大`**(小集最精簡)。

| 卡型 | 乾淨框 | 框尺寸(px) | 階級 | 特效 | 適用上下文 |
|---|---|---|---|---|---|
| **大卡** | `資訊背板_大/{預設,銅色,白銀,黃金,鑽石}.png` | 654×327 | 5 階 | hover:傾斜+珠光+稜鏡+流光 | 個人頁大張資訊卡(C/D) |
| **中板** | `資訊背板_中/資訊背板_預設_小.png` | 677×251 | 單一藍階 | hover:輕微傾斜+珠光 | 狀態列、主選單頭(A/B) |
| **小板** | `資訊背板_小/資訊背板_預設_小.png` | 678×153 | 單一藍階 | 無特效(原圖) | 預覽列、chip、跑馬燈(F/G/H) |

---

## 2. 依 exsample A–I 對應表(主表 · 2026-05-28 拍板)

| 順序 | exsample 變體 | index.html 版位 | 對應卡型 | 素材 | 狀態 |
|---|---|---|---|---|---|
| A | `status-profile` | 狀態列 | **中板** | ✅ `資訊背板_中`(待匯入) | 🟢 可做 |
| B | `menu-header` | 主選單頭 | **中板,做成與 A 一樣** | ✅ `資訊背板_中`(待匯入) | 🟢 可做(同 A) |
| **C** | `profile-info-card--self` | 個人頁(自己) | **大卡** | ✅ 已匯入 | ✅ **已完成** |
| **D** | `profile-info-card--other` | 個人頁(他人) | **大卡** | ✅ 已匯入 | 🟢 可做 |
| E | `player-card--list` | 排行榜列(大) | — | — | 🟡 **待討論暫不處理** |
| F | `player-card--list-mini` | 選單預覽列 | **小板** | ✅ `資訊背板_小`(待匯入) | 🟢 可做(匯入後) |
| G | `player-card--list-chip` | 活躍玩家面板 | **小板** | ✅ `資訊背板_小`(待匯入) | 🟢 可做(匯入後) |
| H | `mq-user` | 跑馬燈 | **小板** | ✅ `資訊背板_小`(待匯入) | 🟢 可做(匯入後) |
| I | `mq-effect3` | 跑馬燈 eff3 | — | — | 🟡 **待討論暫不處理** |

> **素材閘已解除**:中/小框 + 元件都在 `temp/IDCARD_素材_v1`(`資訊背板_中` / `資訊背板_小`),只差匯入專案 `assets/` 即可開工。E / I 經拍板暫緩,不在本輪範圍。

---

## 3. 素材盤點(源 = `temp/IDCARD_素材_v1`)

> 取用順序:**`_小` 先 → 缺再 `_中` → 再 `_大`**。中/小尚未匯入專案 `assets/`,實作前需複製進來(沿用 C 的命名)。

**大卡 `資訊背板_大` — 已匯入 `assets/idcard/`**:5 框(預設/銅色/白銀/黃金/鑽石)、元件(頭像_客製/預設、徽章、成就星星、vip、編輯按鈕、稱號_低等/高等、level bar)。稱號目前用 CSS。

**中板 `資訊背板_中` — 待匯入**:乾淨框 ×1(藍階) + 頭像_中、等級_中(盾牌底+HTML 數字)、成就星星_中、徽章_中、稱號_中、level bar_中。

**小板 `資訊背板_小` — 待匯入**:乾淨框 ×1(藍階) + 頭像_小、等級_小、成就星星_小。(⚠️ 無徽章/稱號/level bar — 小板槽位最精簡)

> 三尺寸一致做法:乾淨框 + 元件 PNG 疊加 + HTML 動態文字(名稱/ID/等級數字/星數)。中/小框僅單一藍階,無 5 階之分。

---

## 4. 現在能動 vs 待討論

**能立即做(素材齊)**
- ✅ C 個人頁自己卡 — 已完成
- 🟢 **D 個人頁他人卡** — 套 C 的大卡規格 + `--profile-other` 隱藏他人不該看的資訊
- 🟢 **A / B 中板** — 套 portable 中板(`hud_medium.png` + 輕微傾斜珠光);B 做成與 A 一樣
- 🟢 **F / G / H 小板** — 先搬 `hud_small.png` 進 `assets/`,再套 portable 小板(原圖無特效)

**待討論暫不處理**
- 🟡 E `player-card--list`、I `mq-effect3`

---

## 5. 待你拍板

1. **下一輪實作順序** — D(大卡他人) / A+B(中板) / F+G+H(小板)三組,先做哪一組?
2. ~~中板/小板原圖直用還是疊字~~ → 已由素材包設計回答:三尺寸統一「乾淨框 + 元件 + HTML 疊字」。

> 註:本檔為**給程式人員的方向/規格參考**,非硬性實作約束。尺寸、槽位、命名以美術素材為準即可,程式端可自行規劃實作細節。

---

## 6. 已完成紀錄

- 2026-05-27 **C · 個人頁自己卡(大卡/鑽石)**:乾淨底框 + 頭像(等級盾)/徽章/成就星/VIP/編輯鈕 + 新手稱號(CSS)+ 名字/ID/exp/Next + CSS 經驗條 + hover 4 效果。依 id_diamond 對位約 87%。已上 Pages(commit 1d290a1)。
